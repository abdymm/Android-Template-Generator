package ${packageName}.${className};

public class ${className}Presenter implements ${className}Contract.Presenter{

	/**
     * Start in presenter for view
     */
    @Override
    public void start() {

    }
		
}